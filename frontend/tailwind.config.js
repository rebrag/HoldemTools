module.exports = {
    plugins: [require("tailwind-scrollbar-hide")],
    theme: {
    extend: {
      animation: {
        spotlight: "spotlight-sweep 2.2s linear infinite",
      },
    },
  },
  };
  